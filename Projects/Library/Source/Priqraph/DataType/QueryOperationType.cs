namespace Priqraph.DataType;

public enum QueryOperationType
{
    GetData = 1,
    GetCount = 2,
    Add = 3,
    Edit = 4,
    Remove = 5,
    Merge = 6,
    Script = 7
}

public enum CommandType
{
    Add,
    Edit,
    Remove,
    Merge
}